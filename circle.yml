general:
  build_dir: .

checkout:
  post:
    - cp .env.example .env
    - sed -i'' 's/example.com/trox.cat/g'

dependencies:
  post:
    - pip install -r requirements.txt
test:
  override:
    - honcho start
