general:
  build_dir: .

checkout:
  pre:
  post:
    - cp .env.example .env
    - sed -i'' 's/example.com/trox.cat/g' .env

test:
  override:
    - pip install -r requirements.txt
    - honcho start
